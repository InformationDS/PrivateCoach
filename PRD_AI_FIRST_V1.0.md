# PrivateCoach 2.0 — AI-First 产品设计文档 v1.0

> 日期：2026-06-16  
> 状态：产品设计定稿  
> 范围：PrivateCoach 从健身记录工具转型为 AI-first 健身对话伙伴  
> 目标用户：个人健身爱好者（单人使用）  
> 参考：PRD_AI_COACH_ASSISTANT_V0.1（产品结构方法论）

---

## 1. 执行摘要

PrivateCoach 2.0 将 app 从"手动健身记录工具"升级为"AI-first 健身对话伙伴"。用户打开 app 看到对话界面，通过自然语言（文字或语音）记录训练、查询进度、获取个性化建议。

核心转变：**用户不再"操作工具"，而是"和 AI 对话"**。AI 对话页成为默认入口，传统页面（日历、分析、仪表盘）保留为深度查看和复杂编辑的落点。

---

## 2. 产品定位

### 2.1 一句话定位

用自然语言记录训练、查看数据、获取个性化建议的 AI 健身伙伴。

### 2.2 产品是什么

- 一个**对话优先**的健身记录 app，AI 对话是主界面
- 用户的健身数据是 AI 的长期记忆，AI 基于真实数据给出分析和建议
- 语音和文字双通道输入，语音为主力
- 深度个性化建议：AI 读取训练历史 + 结合训练科学知识 → 定制化方案

### 2.3 产品不是

- 不是通用 ChatGPT（拒绝与健身无关的问题）
- 不是只会回答健身百科的问答机器人（必须基于用户真实数据）
- 不是自动化系统（所有写入必须用户确认）
- 不是社交/社区产品（纯个人使用）

### 2.4 核心原则

1. AI 对话页是新的默认首页
2. 传统页面保留，作为深度查看、复杂编辑和 AI 不可用时的降级路径
3. AI 可以读数据、总结、建议、生成训练记录草稿
4. AI 不能擅自写入或修改数据库
5. 所有写入动作必须出现确认卡，用户确认后执行
6. 建议和分析必须基于真实训练数据，数据不足时必须明说
7. 对话不持久化，每次打开是全新会话；训练数据库是 AI 的唯一长期记忆
8. 健身建议可深度个性化，但涉及医疗/伤病必须谨慎拒答并引导就医

---

## 3. 目标用户与核心场景

### 3.1 目标用户

个人健身爱好者（当前仅你一人使用），特点：

- 以力量训练为主，有氧为辅
- 训练中更愿意口述，不想填表
- 想知道自己的训练趋势和进步情况
- 遇到平台期或训练疑问时希望得到基于数据的建议
- 不希望复杂的操作流程

### 3.2 核心痛点

1. **记录摩擦大**：训练中停下来填表打断节奏，事后回忆容易遗漏
2. **数据看不懂**：有记录但不知道怎么解读趋势和问题
3. **平台期无解**：某个动作卡住了，不知道从哪里突破
4. **缺乏方向**：不知道下次该练什么、怎么安排训练周期
5. **信息分散**：想看趋势要切到分析页，想看详情要切到历史页

### 3.3 核心场景

| 场景 | 用户行为 | AI 做什么 |
|------|---------|----------|
| **训练中记录** | "今天练胸，卧推 60kg 4×8，上斜飞鸟 15kg 3×12" | 解析 → 展示确认卡 → 等待确认 |
| **补充记录** | "再加一个龙门架夹胸" | 增量更新确认卡 |
| **查询进度** | "卧推这一个月有进步吗" | 查询趋势 → 生成图表卡片 + 解读 |
| **获取建议** | "深蹲卡在 80kg 好久了，怎么办" | 分析历史 → 个性化建议 |
| **查看概况** | "这周练得怎么样" | 本周统计 + 部位分布 |
| **规划训练** | "明天该练什么" | 基于部位轮转和恢复状态建议 |

---

## 4. 信息架构

### 4.1 底部 Tab（3 项）

```
AI助手  |  训练记录  |  日历
```

- **AI 助手**：对话主界面，默认首页
- **训练记录**：原 History 页面，按时间倒序的训练列表，支持搜索
- **日历**：原 Calendar 页面，月/周视图，点击查看当日详情

### 4.2 汉堡菜单（左上角 ☰）

```
┌─────────────┐
│ 📊 仪表盘    │  ← 原 Dashboard
│ 📈 分析报告  │  ← 原 Analysis（趋势+体量+报告）
│ 📝 模板管理  │  ← 原 TemplateList
│ ⚙️ 设置      │  ← 原 Settings
└─────────────┘
```

### 4.3 页面关系

**AI 对话页负责：**
- 理解自然语言
- 记录训练（语音 + 文字 → 结构化草稿）
- 查询训练数据（统计、趋势、对比）
- 生成个性化建议
- 展示确认卡
- 确认后执行写入

**传统页面负责：**
- 可视化查看（日历、分析图表）
- 复杂编辑（手动调整历史数据）
- 深度浏览（训练记录列表）
- 配置管理（模板、设置）
- AI 不可用时的完整手动流程

### 4.4 AI 与传统页面的联动

| 场景 | 路径 |
|------|------|
| 保留在对话中完成 | 记录训练、简单查询、获取建议 |
| 跳转到传统页面 | 点击 AI 回复中的按钮跳转到详情/图表 |
| 从传统页面唤起 AI | 日历页点击某天 →"让 AI 分析这天"；分析页 →"AI 给建议" |

---

## 5. AI 对话页设计

### 5.1 第一屏结构

```
┌──────────────────────────────────┐
│ ☰ AI助手              今日 6/16 │  ← TopBar
├──────────────────────────────────┤
│                                  │
│  ┌─ 今日摘要 ───────────────┐   │
│  │ 👋 欢迎回来               │   │
│  │                          │   │
│  │ 本周已训练 3 次 · 42 组   │   │
│  │ 上次训练：昨天 · 腿部     │   │
│  │ 本周覆盖：胸 ✓ 背 ✓ 腿 ✓ │   │
│  │ 肩部本周还没练            │   │
│  └──────────────────────────┘   │
│                                  │
│  ┌─ 快捷指令（横向滑动）─────┐   │
│  │ [🏋️ 记录训练] [📊 本周进度] │   │
│  │ [💡 获取建议] [📅 打开日历] │   │
│  └──────────────────────────┘   │
│                                  │
│  ┌── 对话区 ────────────────┐   │
│  │                          │   │
│  │  [用户消息气泡]           │   │
│  │  [AI 回复 / 卡片]        │   │
│  │  ...                     │   │
│  │                          │   │
│  └──────────────────────────┘   │
│                                  │
├──────────────────────────────────┤
│                                  │
│       🎤 按住录音                 │  ← 主录音按钮
│                                  │
│  ─────── 或者 ───────            │
│                                  │
│  [📎]  ________________  [📤]   │  ← 辅助文本输入
│        输入消息...                │
└──────────────────────────────────┘
```

### 5.2 默认开场（动态生成，基于数据库）

每次打开 app 时，基于当前训练数据动态生成开场摘要，**绝不空白**。

```text
👋 欢迎回来

本周已训练 3 次 · 共 42 组
上次训练：昨天 · 腿部训练
本周已覆盖部位：胸部、背部、腿部

💡 肩部本周还没练，今天可以考虑。

有什么我可以帮你的？
```

动态规则：

| 数据状态 | 摘要内容 |
|---------|---------|
| 今天已训练 | "今天已完成训练 ✓" |
| 今天未训练 | 显示本周统计 + 上次训练 |
| 本周未训练 | "这周还没开始训练，要开始了吗？" |
| 连续 3 天未训练 | "上次训练是 N 天前，休息够了吗 😄" |
| 某部位本周未覆盖 | "XX 部位本周还没练" |
| 有动作停滞 >4 周 | "XX 动作已停滞 N 周，需要看看吗？" |

### 5.3 快捷指令（动态排序）

输入区上方的横向滑动 chips：

- **指令内容动态生成**，基于当前状态排序
- 默认展示 3-4 个，更多横向滑动查看

动态规则：

| 优先展示 | 条件 |
|---------|------|
| 🏋️ 记录训练 | 始终展示（最高频） |
| 📊 本周进度 | 本周有训练记录时 |
| 💡 获取建议 | 有停滞动作时（前置展示） |
| 📝 补昨天记录 | 昨天有训练但未记录时 |
| 🔄 继续上次对话 | 仅当前会话有未完成的草稿时 |
| 📅 打开日历 | 始终展示（低频） |

### 5.4 输入区设计

采用**大语音按钮 + 小文本输入**的布局：

- **语音按钮**：居中，铜金色实心，占视觉重心。按住说话，松开发送，上滑取消
- **录音状态**：按钮变为波形动画 + "松开发送，上滑取消"提示
- **文本输入**：单行，灰底，发送按钮在右侧
- **📎 按钮**：弹出快捷面板（最近模板列表、最近训练部位）

---

## 6. AI Agent 能力地图

### 6.1 训练记录 Agent

**职责**：将自然语言（语音转文字后的文本，或直接文本输入）解析为结构化训练记录草稿，展示确认卡，用户确认后保存。

**能力**：
- 识别训练类型（力量/有氧）
- 提取训练部位
- 提取动作名称、重量、组数、次数
- 提取训练感受
- 提取有氧参数（时长、距离、心率等）
- 支持一句话多动作（"卧推 60 4×8，飞鸟 15 3×12，夹胸做了几组"）
- 发现模糊信息（"做了几组"→ 组数留空/null）
- 支持追加：已有确认卡时，新的记录内容增量更新到同一张卡
- 支持修改：用户说"把刚才那个卧推改成 5 组"

**典型输入**：
```text
今天练胸，平板卧推 60 公斤 4 组 8 次，
上斜哑铃飞鸟 15 公斤 3 组 12 次，
最后龙门架夹胸做了几组，感觉整体状态还行
```

**确认卡输出**：
```
┌─ 训练记录草稿 ──────────────────┐
│ 🏋️ 胸部训练             6月16日 │
│                                 │
│ 平板卧推    60kg ×4组 ×8次  ✏️ │
│ 上斜哑铃飞鸟 15kg ×3组 ×12次 ✏️ │
│ 龙门架夹胸    --  ×--组 ×--次 ✏️ │
│                                 │
│ 😊 感受：[良好 ▾]              │
│                                 │
│ [+ 添加动作]                    │
│                                 │
│ [✅ 确认保存] [✏️ 编辑] [🗑️ 取消]│
└─────────────────────────────────┘
```

**已有记录时**：
```
今天已有一条训练记录（胸部），你要：
[追加到今天的训练] [覆盖今天的记录] [取消]
```
默认推荐追加。

### 6.2 训练查询 Agent

**职责**：理解用户的查询意图，读取本地数据库，生成简洁回答 + 可选的数据卡片 / 图表卡片。

**支持问题类型**：

| 类别 | 示例 |
|------|------|
| 时间统计 | "这周练了几次？""这个月训练了多少天？" |
| 动作趋势 | "卧推最近有进步吗？""深蹲重量变化大吗？" |
| 部位分布 | "这个月哪个部位练最多？""腿部够不够？" |
| 单次回顾 | "上周三练了什么？""上次卧推多重？" |
| 对比查询 | "这个月和上个月比怎么样？" |
| 容量查询 | "这周总容量多少？" |

**输出类型**：

- **简单查询** → 纯文本回复（不调远程 API，本地 SQL + 模板）
  ```
  本周已训练 3 次（周一胸、周三背、今天腿），共 42 组。
  比上周少了 1 次训练。
  ```

- **趋势查询** → 数据卡片 + 内联图表（Canvas 自绘）
  ```
  📊 卧推趋势 · 近30天
  [折线图：最大重量]
  最大重量：55kg → 60kg ↑9%
  训练容量：↑12%
  趋势：稳步上升 📈
  ```

- **对比查询** → 对比卡片
  ```
  📊 本月 vs 上月
  训练天数：12天 vs 14天 ↓14%
  总组数：168组 vs 182组 ↓8%
  总容量：↑5%（单次训练效率提升）
  ```

**处理逻辑**：
1. 识别查询意图 → 提取实体（动作名、时间范围、指标）
2. 简单查询 → 本地 Repository 直接返回 → 模板拼回复文本
3. 复杂查询 → 本地数据聚合成 context → 发给 Gemini → 自然语言解读 + 图表渲染

### 6.3 训练建议 Agent

**职责**：基于用户真实训练数据 + AI 训练科学知识，给出深度个性化建议。不直接写数据库。

**能力**：

| 场景 | 触发示例 | AI 行为 |
|------|---------|---------|
| **平台期突破** | "卧推卡在 60kg 好久了" | 分析该动作 12 周趋势 → 识别停滞模式 → 给定制化建议 |
| **训练安排** | "明天该练什么" | 查看部位轮转状态 + 恢复时间 → 建议训练部位 |
| **弱点分析** | "我训练有什么问题吗" | 分析部位平衡、频率、容量 → 指出不足 |
| **通用训练问题** | "增肌应该怎么安排组数" | 通用知识回答，但不做医疗建议 |
| **动作纠错** | "卧推肩膀疼" | 分析可能原因 → 给动作调整建议 → 提示必要时就医 |

**建议输出卡片**：
```
💡 卧推平台期分析

📊 你的卧推现状：
· 近8周最大重量停滞在 60kg
· 训练频率：平均每周 1.2 次（偏低）
· 辅助动作覆盖：飞鸟 ✓、夹胸 ✓，但缺少三头肌孤立训练

🔍 分析：
你的胸大肌训练量足够，但卧推锁定期依赖三头肌力量。
三头肌训练不足可能是锁定期瓶颈。

💪 建议：
1. 每周增加 1 次轻重量卧推日（50kg 3×10-12），提高技术容量
2. 加入窄距卧推或双杠臂屈伸加强三头肌
3. 检查卧推起桥和肩胛收紧是否到位

需要我帮你在下周计划中加入这些吗？
[👍 有帮助] [👎 不相关] [📋 生成训练计划]
```

**数据不足时**：
```
你目前只有 2 条深蹲训练记录，数据不足以分析趋势。
建议至少积累 4-6 次深蹲训练后再分析。
不过，关于深蹲平台期，通用建议是：...
```

### 6.4 运营复盘 Agent

**职责**：按周/月汇总训练情况，生成文字总结 + 可视化卡片。

**支持问题**：
- "帮我总结这周训练"
- "这个月练得怎么样"
- "最近一个月有什么变化"

**输出示例**：
```
📊 本周训练总结（6/10 - 6/16）

训练天数：4 天 · 共 56 组
总容量：12,340 kg（↑8% vs 上周）

部位分布：
胸 ████████░░ 35%
背 ██████░░░░ 25%
腿 ████░░░░░░ 15%
肩 ███░░░░░░░ 10%
其他 ████░░░░░░ 15%

进步最快：卧推 ↑5kg、深蹲容量 ↑12%
注意：腿部训练频率偏低（1次/周），建议≥2次/周

[查看详细] [获取建议]
```

### 6.5 快速操作 Agent

**职责**：处理不涉及 AI 解析的轻量操作。

**支持操作**：

| 操作 | 示例 | 处理 |
|------|------|------|
| 删除记录 | "把上周五那条记录删了" | 二次确认 → 执行 |
| 创建模板 | "把今天的训练存为模板，叫推胸日" | 创建模板 → 确认 |
| 修改设置 | 暂不支持，引导到设置页 | 跳转 |

---

## 7. 自然语言任务分类

### 7.1 任务分类总览

```
用户输入
    │
    ▼
┌─────────────┐
│  意图识别    │
└──┬──────────┘
   │
   ├── 记录类 → AI 解析 → 确认卡 → 用户确认 → 写入
   ├── 查询类 → 本地读取 → 回复（纯文本/卡片/图表）
   ├── 建议类 → 数据聚合 + AI → 建议卡片
   ├── 复盘类 → 数据聚合 + AI 生成 → 复盘卡片
   ├── 操作类 → 确认 → 本地执行
   ├── 模糊类 → 追问澄清
   ├── 无关类 → 拒答 + 引导回健身
   └── 高风险类 → 拒答 + 引导
```

### 7.2 记录类

**特征**：写入训练数据，必须确认。支持追加和覆盖。

**例子**：
```
"今天练胸，卧推 60kg 4×8，飞鸟 15kg 3×12"
"记录一下，刚跑了 5 公里，配速 5 分半"
"刚才那个卧推再加一组"
"把今天训练的感受改成疲惫"
```

**缺失信息处理**：
```
用户："今天练了卧推"
AI："收到，需要补充一下：重量、组数、次数是多少？还有练了其他动作吗？"
```

### 7.3 查询类

**特征**：只读，不需要确认，可以直接回答。简单查询纯本地，复杂查询本地聚合 + AI 解读。

**例子**：
```
"这周练了几次？"
"卧推最近有进步吗？"
"上个月和这个月比怎么样？"
"上次练腿是什么时候？"
```

### 7.4 建议类

**特征**：读取训练数据 + AI 训练科学知识 → 个性化建议。不写入。

**例子**：
```
"卧推卡住了怎么办？"
"明天练什么？"
"我训练有什么问题吗？"
"帮我看看我的训练计划"
```

### 7.5 复盘类

**特征**：时间范围聚合 + 多维度统计 + AI 生成解读。

**例子**：
```
"帮我总结这周"
"这个月练得怎么样"
"最近一个月有什么变化"
```

### 7.6 模糊类

**特征**：意图不明确，不拒答，追问澄清。

**例子**：
```
用户："看看我的训练"
AI："你想看哪方面？"
    "1. 这周的训练统计"
    "2. 某个动作的趋势（比如卧推）"
    "3. 身体部位分布"
    "4. 生成训练建议"
```

### 7.7 无关类

**特征**：与健身训练无关的问题，拒答并引导回业务。

**例子**：
```
"帮我写一首诗"
→ "我专注于帮你记录和分析训练。你可以试试说'记录今天的训练'或'这周练得怎么样？'"

"推荐一部电影"
→ "我只能帮你做健身相关的事哦。需要记录训练或查看数据吗？"
```

### 7.8 高风险类

**特征**：涉及医疗诊断、伤病判断等超出能力范围的问题。

**例子**：
```
"膝盖疼还要不要继续练深蹲？"
→ "膝盖疼痛可能有很多原因，我无法替代医生判断。
    建议先暂停深蹲，找运动康复科或骨科医生评估。
    如果你想记录这个情况，我可以帮你在备注里标记。"
```

---

## 8. 确认卡设计

### 8.1 确认卡定位

确认卡是 AI 写入操作的**安全闸门**。用户必须看清楚 AI 要写什么、是否准确，再决定是否确认。

### 8.2 通用规则

- 查询类、建议类**不需要**确认卡
- 任何写入数据库的动作**必须**有确认卡
- 确认卡中的内容必须结构化，不能只是一段自然语言
- 语音输入触发的记录，确认卡中展示 AI 听到的原文
- 确认卡支持内联编辑（点击 ✏️ 直接修改字段）
- 确认卡支持增量追加（用户说"再加一个 XX"→ 卡片新增一行）

### 8.3 训练记录确认卡

```
┌─ 训练记录草稿 ──────────────────┐
│ 🏋️ 胸部训练             6月16日 │
│                                 │
│ 平板卧推    60kg ×4组 ×8次  ✏️ │
│ 上斜哑铃飞鸟 15kg ×3组 ×12次 ✏️ │
│ 龙门架夹胸   --kg ×--组 ×--次 ✏️ │
│                                 │
│ 😊 感受：[良好 ▾]              │
│                                 │
│ [+ 添加动作]                    │
│                                 │
│ [✅ 确认保存] [✏️ 编辑] [🗑️ 取消]│
└─────────────────────────────────┘
```

**追加场景**：
```
用户："再加一个双杠臂屈伸 3×10"
→ 卡片动态新增一行"双杠臂屈伸 --kg ×3组 ×10次"
→ 不重新生成整张卡片
```

### 8.4 删除确认卡

```
┌─ 确认删除 ──────────────────────┐
│ ⚠️ 将永久删除以下训练记录：       │
│                                 │
│ 6月14日 · 背部训练              │
│ 引体向上、杠铃划船、高位下拉     │
│                                 │
│ 此操作不可恢复。                 │
│                                 │
│ [⚠️ 确认删除] [取消]            │
└─────────────────────────────────┘
```

### 8.5 覆盖确认卡（同日已有记录时）

```
┌─ 今天已有训练记录 ──────────────┐
│ 今天已有一条训练记录：           │
│ 🏋️ 胸部 · 6月16日              │
│ 平板卧推 60kg×4×8               │
│                                 │
│ 新记录的动作将：                 │
│ [📎 追加到今天的训练]  ← 推荐   │
│ [🔄 覆盖今天的记录]             │
│ [取消]                          │
└─────────────────────────────────┘
```

---

## 9. AI 权限分级

### 9.1 Level 1：只读查询

**允许**：读取训练记录、读取模板、读取设置、生成摘要和统计

**不允许**：任何写入操作

**对应能力**：查询 Agent、建议 Agent、复盘 Agent 的数据读取部分

### 9.2 Level 2：生成草稿

**允许**：生成训练记录草稿、生成建议文本、生成复盘文本

**不允许**：自动保存任何数据

**对应能力**：记录 Agent 的解析阶段、建议 Agent 的生成阶段

### 9.3 Level 3：确认后写入

**允许**：用户点击确认后保存训练记录、追加训练记录、覆盖训练记录、删除训练记录、创建模板

**对应能力**：记录 Agent 的保存阶段、操作 Agent

### 9.4 Level 4：自动执行

**本方案不开放 Level 4**。

AI 不允许自动执行任何写入动作。

---

## 10. 严禁 AI 操作

以下操作无论用户如何要求，AI 都不能执行，也不能生成确认卡：

### 10.1 数据破坏类

- 批量删除训练记录
- 清空全部训练数据
- 删除模板（需引导到模板管理页手动操作）
- 修改历史训练记录的日期（可能导致数据一致性问题）

### 10.2 设置与系统类

- 修改 AI API 配置
- 修改数据库配置
- 导出全量数据（需引导到设置页）
- 导入外部数据（需引导到设置页）

### 10.3 医疗与健康风险类

- 对伤病做诊断性结论
- 建议用户忽视疼痛继续训练
- 推荐药物或补剂

**拒绝示例**：
```
"帮我把所有训练记录删了"
→ "出于安全考虑，我不能批量删除数据。
   你可以在训练记录页面逐条删除，或在设置中导出备份。"
```

---

## 11. 数据记忆设计

### 11.1 核心原则

- **对话不持久化**：每次打开 app 是全新会话，对话历史不保存
- **训练数据库 = AI 长期记忆**：AI 的所有上下文来自 Room 数据库中的训练数据
- AI 只能基于数据库中的真实数据回答，数据不足时必须说明

### 11.2 会话初始化（冷启动 Context）

每次新会话开始时，一次性查询以下数据注入 AI system prompt：

```
📋 会话 Context（注入 system prompt）：
├── 用户训练画像
│   ├── 总训练次数、开始训练至今
│   ├── 近 4 周训练频率（每周几天）
│   ├── 最常练的部位 Top 3
│   └── 常用动作名称列表
├── 本周状态
│   ├── 本周已训练天数
│   ├── 最近一次训练（日期 + 部位 + 动作摘要）
│   └── 本周各部位覆盖情况（哪些练了、哪些没练）
├── 关键趋势
│   ├── 进步最快的动作 Top 3（近 4 周容量增长率）
│   └── 停滞的动作（超 4 周重量无增长）
└── 用户偏好
    ├── 常用模板列表
    └── 偏好训练部位（从历史频率推断）
```

### 11.3 对话中的动态数据注入

对于建议类和复杂查询，在初始化 context 外，还需动态注入更详细的数据：

```
用户："深蹲卡住了怎么办"
→ 额外注入：
  · 深蹲近 12 周每次训练的重量、组数、次数
  · 腿部训练频率（每周几次）
  · 辅助动作覆盖（腿举、腿弯举、臀推等）
  · 近 4 周容量趋势
```

### 11.4 防止 AI 胡说

AI 不允许：
- 没查到数据却说"最近训练稳定"
- 没查到重量变化却说"力量提升"
- 数据不足时给确定性结论

**数据不足示例**：
```
你目前只有 3 条深蹲训练记录，最近一次是 2 周前。
以下分析仅基于这 3 条记录，可能不够全面。
```

---

## 12. 标准对话流程

### 12.1 总流程

```
用户输入（文字/语音）
    → 语音转文字（如为语音）
    → 意图识别
    → 安全边界判断
    → 实体/参数提取
    → 缺失信息追问（如需要）
    → 业务校验
    → 生成回复 / 草稿 / 确认卡
    → 用户确认（写入类）
    → 执行业务操作
    → 返回结果
```

### 12.2 查询类流程（简化）

```
用户："这周练了几次"
    → 意图：查询
    → 参数：时间范围=本周, 指标=训练天数
    → 本地 SQL 查询
    → 模板拼回复文本
    → 直接回复（不调远程 API）
```

### 12.3 记录类流程

```
用户："今天练胸，卧推 60 4×8，飞鸟 15 3×12"
    → 意图：记录
    → 发送文本到 Gemini（解析 prompt）
    → 返回结构化 AiParsedResult
    → 检查同日是否已有记录
    → 如有 → 展示追加/覆盖选择
    → 如无 → 展示确认卡
    → 用户点击确认 → 写入 Room
    → 返回成功结果
```

### 12.4 建议类流程

```
用户："卧推卡在 60 好久了"
    → 意图：建议
    → 提取动作：平板卧推
    → 查询该动作 12 周详细数据
    → 查询关联部位（胸部）训练频率
    → 聚合数据 → 注入 system prompt
    → 发送到 Gemini（建议生成 prompt）
    → 返回建议卡片
```

---

## 13. 消息 Bubble 类型

| 类型 | 触发场景 | 交互 |
|------|---------|------|
| UserTextBubble | 用户文本输入 | 长按复制 |
| UserVoiceBubble | 用户语音输入 | 点击播放/暂停，显示转写文本 |
| AiTextBubble | AI 纯文本/Markdown 回复 | — |
| ConfirmCard | AI 解析训练结果 | 内联编辑、确认/取消、追加 |
| DataCard | 数据查询结果 | 点击展开详情、跳转 |
| ChartCard | 趋势/分布查询（含 Canvas 图表） | 切换指标、展开 |
| AdviceCard | AI 建议 | 有帮助/不相关反馈 |
| SummaryCard | 复盘/总结 | 点击展开详情 |
| QuickActionChips | 欢迎页快捷指令 | 点击触发预设输入 |
| SystemMessage | 状态提示、错误、数据不足 | — |

---

## 14. 现有功能处置

### 14.1 保留并整合

| 现有页面 | 新位置 | 调整 |
|---------|--------|------|
| CalendarScreen | 底部 Tab 3 | 基本不变 |
| HistoryScreen | 底部 Tab 2 | 基本不变；搜索大部分由对话替代 |
| AnalysisScreen | 汉堡菜单 | 基本不变；趋势+体量+报告三Tab |
| DashboardScreen | 汉堡菜单 | 降级为辅助汇总页 |
| TemplateListScreen | 汉堡菜单 | 基本不变 |
| SettingsScreen | 汉堡菜单 | 增加对话偏好设置 |
| WorkoutDetailScreen | 从 History/Calendar 进入 | 基本不变 |

### 14.2 对话化替代

| 旧页面 | 替代方式 |
|--------|---------|
| RecordScreen（三Tab：语音/文字/手动） | 对话输入区统一替代 |
| ConfirmResultScreen（编辑AI解析结果） | 确认卡内联编辑替代 |
| ManualEntryScreen | 保留作为深度编辑入口，对话中"编辑"按钮可跳转 |

### 14.3 保留不变

| 模块 | 处理 |
|------|------|
| AudioRecorder + AudioPlayer | 直接复用到对话页语音输入 |
| GeminiApiService | 扩展 prompt 体系 |
| Room 数据库（5 实体） | 不变 |
| Canvas 图表组件 | 复用，嵌入 ChartCard |
| 数据导出/导入 | 保持不变，设置页入口 |
| Widget | 保持不变，点击跳转改为对话页 |
| 模板系统 | 保留管理页面，对话中可引用模板 |

---

## 15. 功能需求清单

### 15.1 AI 对话首页

- [ ] 作为 App 默认首页（底部 Tab 1）
- [ ] 动态生成今日摘要（基于训练数据）
- [ ] 横向滑动快捷指令（动态排序）
- [ ] 文本输入
- [ ] 语音输入（按住录音）
- [ ] 消息 Bubble 渲染（10 种类型）
- [ ] 确认卡交互（编辑/确认/取消/追加）
- [ ] 跳转传统页面按钮
- [ ] 新会话时展示开场摘要

### 15.2 意图识别

- [ ] 识别记录类
- [ ] 识别查询类
- [ ] 识别建议类
- [ ] 识别复盘类
- [ ] 识别操作类
- [ ] 识别模糊问题（追问）
- [ ] 识别无关问题（拒答+引导）
- [ ] 识别高风险问题（拒答+引导）

### 15.3 训练记录 Agent

- [ ] 文本解析为结构化训练记录
- [ ] 语音 → 转文字 → 解析为结构化训练记录
- [ ] 支持一句话多动作
- [ ] 支持有氧运动解析
- [ ] 生成确认卡
- [ ] 确认卡内联编辑
- [ ] 增量追加（多轮对话追加动作）
- [ ] 修改未保存的草稿
- [ ] 同日已有记录时提示追加/覆盖
- [ ] 确认后保存到 Room
- [ ] 保存成功反馈

### 15.4 训练查询 Agent

- [ ] 时间统计查询（本周/本月训练天数等）
- [ ] 动作趋势查询（含折线图）
- [ ] 部位分布查询（含饼图）
- [ ] 单次训练回顾
- [ ] 周期对比查询（本月 vs 上月）
- [ ] 简单查询本地完成（不调远程 API）
- [ ] 复杂查询数据 + AI 解读
- [ ] 数据不足时明确说明

### 15.5 训练建议 Agent

- [ ] 平台期分析和突破建议
- [ ] 训练安排建议（"明天该练什么"）
- [ ] 部位平衡分析
- [ ] 基于数据的个性化建议生成
- [ ] 通用训练知识问答
- [ ] 数据不足时说明但给出通用建议
- [ ] 建议卡片（有帮助/不相关反馈）

### 15.6 运营复盘 Agent

- [ ] 本周训练总结
- [ ] 本月训练总结
- [ ] 多维度统计（天数、组数、容量、部位分布）
- [ ] 进步/停滞动作识别
- [ ] 改进建议

### 15.7 安全与边界

- [ ] 所有写入操作必须确认
- [ ] 严禁操作不可执行
- [ ] 无关问题拒答 + 引导
- [ ] 高风险健康问题拒答 + 引导
- [ ] 数据不足时说明不确定性

### 15.8 导航与联动

- [ ] 底部 3 Tab 导航（AI对话 | 训练记录 | 日历）
- [ ] 汉堡菜单（仪表盘、分析报告、模板管理、设置）
- [ ] AI 回复中的跳转按钮
- [ ] 传统页面中唤起 AI 的入口

### 15.9 降级与设置

- [ ] AI 不可用时传统页面完整可用
- [ ] 设置页显示 AI 状态
- [ ] 手动录入作为兜底入口
- [ ] API Key 配置保持不变

---

## 16. 非功能要求

### 16.1 可靠性

- AI 失败不能影响传统页面功能
- 写入操作由 Room 事务保障
- 确认卡 payload 可复核
- 语音转写失败时提示重试或改用文字

### 16.2 响应速度

- 查询类（本地）：即时返回，体感零延迟
- 记录类（远程 AI 解析）：< 5 秒
- 建议类（远程 AI 生成）：< 10 秒（可先提示"正在分析"）
- 对话首页渲染：即时

### 16.3 可解释性

- 分析类回答展示数据依据
- 数据不足明确说明范围限制
- 不使用无来源的确定性判断

### 16.4 安全性

- 严禁操作必须拦截
- API Key 本地加密存储（Keystore）
- 训练数据纯本地，不上传至第三方（仅 AI 调用时发送解析 prompt）

### 16.5 离线可用

- 查询类、日历、历史、分析全部本地数据驱动，离线可用
- 记录类（AI 解析）、建议类需网络
- 离线时对话页降级显示说明 + 引导到手动录入

---

## 17. 验收标准

项目完成后应满足：

1. 打开 App 默认进入 AI 对话页，展示动态今日摘要
2. 可以用文本输入自然语言记录训练
3. 可以按住录音按钮口述训练内容
4. AI 解析结果以确认卡形式展示，用户确认后保存
5. 支持多轮对话追加训练内容到同一确认卡
6. 可以自然语言查询训练统计（"这周练了几次"等）
7. 简单查询纯本地完成，不调远程 API
8. 趋势查询展示内联图表卡片
9. AI 可以基于真实训练数据给出个性化建议
10. AI 可以生成周/月训练复盘
11. 所有写入操作必须用户确认
12. 无关问题被拒答并引导回健身场景
13. 高风险健康问题被拒答并引导就医
14. 数据不足时 AI 明确说明
15. 底部 3 Tab：AI 对话、训练记录、日历，均可用
16. 汉堡菜单 4 入口：仪表盘、分析报告、模板管理、设置，均可用
17. 传统页面可独立使用，不依赖 AI
18. 离线时传统功能完整可用

---

## 18. 构建检查点

这不是公开版本拆分，而是完整项目的施工顺序：

1. AI 对话页 UI（消息列表 + Bubble 组件 + 输入区）
2. 意图识别引擎（规则引擎）
3. 确认卡机制
4. 训练记录 Agent（文本 → 确认卡 → 保存）
5. 会话初始化 Context 构建
6. 语音输入集成
7. 训练查询 Agent（本地 + 远程）
8. 导航重构（3 Tab + 汉堡菜单）
9. 训练建议 Agent
10. 运营复盘 Agent
11. 传统页面联动（跳转、唤起 AI）
12. AI 设置与降级状态
13. 全流程体验打磨

施工原则：
- 产品范围按完整方案设计
- 代码按依赖顺序构建
- 每个检查点可独立验证

---

## 19. 决策记录

当前已确认决策：

1. 功能范围：录入 + 查询走对话，分析/日历页面保留（方案 B）
2. 建议深度：深度个性化建议，结合训练数据 + AI 自带训练科学知识（方案 C）
3. 对话历史：不保存，每次打开全新会话；训练数据库 = AI 长期记忆
4. 现有功能处置：RecordScreen + ConfirmResultScreen 对话化替代，其他保留（方案 B）
5. 导航方案：底部 3 Tab（AI对话 | 训练记录 | 日历）+ 汉堡菜单（仪表盘、分析、模板、设置）
6. AI 知识来源：直接使用外接 API（Gemini）的自带训练科学知识
7. 快捷入口：动态生成，基于当前训练状态排序
8. 响应方式：一次性返回，不做流式
9. UI 风格：沿用藏蓝 × 铜金极简线条风格，对话页扩展专用 Bubble 配色
10. 无需新增数据库表，现有 5 表足够
