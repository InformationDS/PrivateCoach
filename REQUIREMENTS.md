# PrivateCoach — 健身记录 App 需求文档

> 版本: v0.4 | 日期: 2026-06-04 | 状态: 需求已定稿，待进入技术架构

---

## 1. 项目定位

一款**纯个人使用**的健身记录工具，专注于**力量训练**和**有氧运动**两大场景。核心理念：**语音随手记，AI 自动理** — 用户只需口语化描述训练内容，AI 自动提取结构化数据并生成训练日志。

- **目标用户**: 个人健身爱好者（当前仅你一人使用）
- **平台**: Android
- **存储策略**: **本地优先（Room 数据库）**，后期扩展云端同步（数据模型预留同步字段）
- **网络依赖**: AI 解析需要网络（调用多模态 API）；其余功能完全离线可用
- **AI 服务**: 用户自供多模态 API（支持 audio-in），一次调用同时完成语音识别 + 结构化提取 + 日志生成
- **录入方式**: 语音输入为主（多模态 AI 解析），文字输入为辅（直接发送文本到 AI 解析，或手动填表）
- **UI 风格**: 深色系唯一（藏蓝 × 铜金），极简线条风

---

## 2. 核心功能需求

### 2.1 语音输入 & 记录

#### 2.1.1 语音录入
| 项目 | 描述 |
|------|------|
| 触发方式 | 首页显眼的录音按钮（FAB），长按/点击开始录音 |
| 输入特点 | **口语化、非结构化** — 用户可以像聊天一样说，不要求特定格式 |
| 典型输入示例 | *"今天练胸，先做了平板卧推 60公斤 4组 每组8次，然后上斜哑铃飞鸟 用的15公斤 做了3组12次，最后龙门架夹胸做了几组，感觉整体状态还行"* |
| 中途暂停 | 支持暂停/继续录音（训练中可能分多段说） |
| 录音历史 | 保留原始录音文件，可回放 |

#### 2.1.1b 文字输入（备用通道）

语音是主力，但必须保留文字输入作为兜底和补充：

| 场景 | 说明 |
|------|------|
| **文字直输** | 用户可打字描述训练内容，发送到同一 AI 做解析（与语音输出格式一致） |
| **环境受限** | 健身房太吵、不方便说话时，文字输入是必备降级方案 |
| **手动补充** | AI 解析后，用户可手动修正任何字段（动作名、重量、次数等） |
| **纯手动录入** | 不经过 AI，直接用表单填入：训练类型 → 动作 → 组数/次数/重量 |

#### 2.1.2 语音识别 & AI 解析（多模态 AI 一步出）

**已定方案：多模态 AI 直出**

用户提供多模态 API（支持 audio-in），一条音频进去，结构化数据 + 日志文本出来。省去 ASR → 文本解析的中间链路。

```
录音文件（.m4a / .wav）
    │
    ▼
多模态 AI API（用户提供）
    │  单次调用
    ├──► 结构化数据（JSON）
    └──► 自然语言日志（文本）
    │
    ▼
存入本地 Room 数据库 + 展示给用户确认
```

| 项目 | 说明 |
|------|------|
| 模型 | 用户自供多模态 API（Gemini / GPT-4o 等支持 audio-in 的模型） |
| 输入 | 原始音频文件（口语化中文，不要求结构化） |
| 输出 | 结构化 JSON + Markdown 日志文本，一次调用同时产出 |
| 优势 | 减少链路，AI 可结合语气判断感受（"还行" vs "很棒"），对非标准表述容错率高 |
| API 管理 | 设置页填入 API Key + Endpoint，本地加密存储 |

**音频处理策略**：
- 训练中可分段录音（一组练完说一段），也可以练完一次说完
- 支持追加录音：新音频 + 已有上下文 → AI 合并更新结果
- 最长单次录音 3 分钟（覆盖一次完整训练描述绰绰有余）

#### 2.1.3 AI 解析输出规范

多模态 AI 一次调用同时输出两部分：

**A. 结构化数据（JSON — 无预设动作库，纯 AI 判断）**
```
{
  "type": "strength",          // strength | cardio — AI 自动判断
  "bodyPart": "胸部",          // ⚠️ AI 自动识别训练部位
  "exercises": [
    { "name": "平板卧推", "weight": 60, "unit": "kg", "sets": 4, "reps": 8 },
    { "name": "上斜哑铃飞鸟", "weight": 15, "unit": "kg", "sets": 3, "reps": 12 },
    { "name": "龙门架夹胸", "sets": null, "reps": null }
  ],
  "feeling": "良好",
  "notes": "整体状态还行"
}
```

**B. 有氧运动结构化**
```
{
  "type": "cardio",
  "cardioType": "跑步",
  "duration": 1800,            // 秒
  "distance": 5.2,             // km
  "avgPace": "5'46\"",
  "feeling": "轻松"
}
```

**C. 自然语言日志（Markdown）**
```
今日胸部训练，共完成 3 个动作：
1. 平板卧推 — 60kg × 4组 × 8次
2. 上斜哑铃飞鸟 — 15kg × 3组 × 12次
3. 龙门架夹胸 — 组数次数组未记录
训练感受：整体状态还行。
```

**解析原则**：
- 无预设动作库，AI 自行从口语中提取动作名 + 推断训练部位
- 模糊信息（"做了几组"）对应字段留 `null`，不强行填充
- 自动区分力量 vs 有氧训练类型

#### 2.1.4 手动录入（兜底方案）
- 提供传统表单录入：选择训练类型 → 添加动作 → 填重量/组数/次数
- 支持编辑 AI 解析结果

---

### 2.2 日历视图

| 特性 | 描述 |
|------|------|
| 月视图 | 默认月历，有训练记录的日子用**色点/图标**标记 |
| 训练类型区分 | 力量训练和​有氧训练用不同颜色标记 |
| 点击日期 | 弹出当日训练概览（动作摘要 + 训练感受） |
| 切换周视图 | 可切换到周视图，显示更多详情 |
| 快捷跳转 | 可快速跳到"今天"，支持前后翻月 |
| 统计卡片 | 日历上方显示本月汇总：训练天数、总组数、总时长 |

### 2.2b 训练模板

允许用户将某次训练存为模板，快速复用固定动作组合。

| 特性 | 描述 |
|------|------|
| 创建模板 | 从已有训练记录 → "保存为模板" → 命名（如"推胸日"、"拉背日"、"5km跑"） |
| 使用模板 | 手动录入时可选择模板，自动填充动作列表，用户只需改重量/次数 |
| 模板管理 | 列表展示、编辑、删除 |
| 语音 + 模板 | 语音录入时 AI 可引用模板名（如"今天按推胸日模板练的，卧推加了5公斤"） |

### 2.2c 桌面 Widget

| 特性 | 描述 |
|------|------|
| 尺寸 | 2×1 或 4×1 紧凑小部件 |
| 显示 | 本周训练天数 / 上次训练距今 / 今日是否有训练 |
| 核心操作 | 一键启动录音（Widget 上直接点录音按钮，跳转 App 录音页） |
| 风格 | 深色透明背景，铜金色文字，1px 边框，与主 App 风格统一 |

---

### 2.3 数据分析

#### 2.3.1 趋势图
- **单动作趋势**: 选择一个动作（如"平板卧推"），查看其重量/次数随时间变化曲线
- **对比**: 可叠加多个动作的趋势线进行对比
- **时间范围切换**: 近1个月 / 3个月 / 6个月 / 全部
- **展示指标**: 最大重量、总容量（重量×次数×组数）、平均次数

#### 2.3.2 体量统计
- **周/月训练容量汇总**: 每周总训练组数、总容量（kg × reps × sets 求和）
- **身体部位分布**: 饼图/柱状图展示各部位训练频率（胸/背/腿/肩/手臂/有氧）
- **训练频率**: 每周训练天数趋势

#### 2.3.3 报告生成
- **周报/月报**: AI 汇总本周/本月训练情况，生成文字总结
- **内容示例**:
  - 本周训练 X 天，共 Y 个动作
  - 相比上周，容量变化 +X%
  - 最常练的部位排名
  - 进步最大的动作（重量/容量增长 Top 3）
  - 可改进的点（如某部位训练不足）
- **导出**: 支持导出为图片/文本分享

---

### 2.4 训练记录管理

- **记录列表**: 按时间倒序展示所有训练记录
- **详情页**: 查看某次训练的完整信息（结构化数据 + AI 生成的日志文本）
- **编辑**: 修改任何字段（动作名、重量、次数等）
- **删除**: 删除单条记录（确认弹窗）
- **搜索**: 按动作名、日期范围搜索

---

## 3. 非功能需求

| 类别 | 要求 |
|------|------|
| 响应速度 | 语音识别 + AI 解析总时长 < 5 秒（体感不卡顿） |
| 数据安全 | 本地存储，API Key 本地加密保存（Android Keystore）；后期云端同步需端到端加密 |
| 备份恢复 | 导出/导入 JSON 格式；后期版本支持云端自动同步 |
| 云端同步 | **前期不做**，但数据模型预留 `syncId`、`updatedAt` 等字段，为后期迁移做准备 |
| 界面语言 | 中文 |
| 最低系统 | Android 10 (API 29) |
| UI 框架 | Jetpack Compose（Material 3，深色主题） |
| 主题 | **深色系默认且唯一**，不做亮/暗切换 |

---

## 4. 数据模型初稿

```
Workout (训练记录)
├── id: Long (本地主键)
├── syncId: String?                // 云端同步 ID（预留）
├── date: LocalDate
├── type: WorkoutType (STRENGTH | CARDIO)
├── bodyPart: String?
├── feeling: String?               // 训练感受
├── aiSummary: String?             // AI 生成的文本总结
├── rawTranscript: String?         // 语音/文字输入原文
├── audioFilePath: String?         // 原始录音路径（文字输入时为空）
├── inputMode: InputMode           // VOICE | TEXT | MANUAL
├── templateId: Long?              // 来源模板 ID（可选）
├── createdAt: Instant
├── updatedAt: Instant             // 云端同步用（预留）
└── exercises: List<Exercise>

TrainingTemplate (训练模板)
├── id: Long
├── name: String                   // "推胸日"、"拉背日"
├── type: WorkoutType
├── bodyPart: String?
├── exercises: List<TemplateExercise>  // 预设动作列表（不含重量/次数）
├── sortOrder: Int
└── createdAt: Instant

TemplateExercise (模板动作)
├── id: Long
├── templateId: Long
├── name: String                   // 动作名
├── weightUnit: String             // kg | lb
├── sortOrder: Int
└── notes: String?

Exercise (训练动作)
├── id: Long
├── workoutId: Long
├── name: String                   // 动作名
├── weight: Double?                // 重量
├── weightUnit: String             // kg | lb
├── sets: Int?                     // 组数
├── reps: Int?                     // 次数
├── duration: Int?                 // 有氧时长（秒）
├── distance: Double?              // 有氧距离（km）
├── sortOrder: Int                 // 排序
└── notes: String?

CardioDetail (有氧详情, 可扩展)
├── exerciseId: Long
├── durationSeconds: Int
├── distanceKm: Double?
├── avgHeartRate: Int?
├── calories: Int?
└── cardioType: String?            // 跑步/游泳/骑行...
```

---

## 5. UI 设计原则

### 5.1 配色方案：藏蓝 × 铜金

**不提供亮/暗切换**，以深藏蓝为唯一视觉基调。

| 角色 | 色值 | 用途 |
|------|------|------|
| 主背景 | `#0F172A` (深藏蓝) | 页面底色 |
| 卡片/面板 | `#1A2332` | 卡片、弹窗、底部栏 |
| 分割线 | `#243044` | 1px 细线分隔 |
| **强调色** | `#D4A574` (铜金/暖沙) | FAB、选中态、关键数据高亮 |
| 力量标记 | `#D4A574` | 日历点、标签、趋势线 |
| 有氧标记 | `#7DD3D4` (浅青) | 日历点、标签、趋势线 |
| 主文字 | `#E8EDF2` | 标题、正文 |
| 次文字 | `#8E9BAE` | 说明文字、时间戳 |
| 禁用/极弱 | `#4A5568` | 占位符、非活跃态 |

### 5.2 极简线条风格规范

整体走**克制、精密、留白**的极简路线，用线条而非色块来定义空间。

| 元素 | 规范 |
|------|------|
| **图标** | 细线图标（stroke 1.5px），禁用填充图标 |
| **分隔** | 仅用 `1px` 细线（`#243044`），不用色块面区分 |
| **卡片圆角** | 4dp（几乎直角），禁用大圆角 |
| **卡片边框** | 1px 边框替代阴影（无 elevation 风格） |
| **字重层级** | 标题 Light(300) / 正文 Regular(400) / 数据 Medium(500)，禁用 Bold |
| **间距** | 大量留白，内容密度低，模块间距 ≥ 24dp |
| **图表** | 趋势线 1-2dp，坐标轴淡化，网格线极少或无线 |
| **FAB** | 方形圆角 8dp，几何感，不用正圆形 |
| **导航** | 底部导航栏用细线上缘分隔，选中态仅颜色变化不加粗 |

### 5.3 页面结构

```
├── 首页 (Dashboard)
│   ├── 今日快速状态
│   ├── 语音录入按钮 (FAB，强调色，最显眼)
│   └── 最近训练摘要
│
├── 日历 (Calendar)
│   ├── 月视图 / 周视图切换
│   └── 点击日期 → 当日训练详情
│
├── 分析 (Analysis)
│   ├── 趋势图 Tab
│   ├── 体量统计 Tab
│   └── 报告 Tab
│
├── 记录列表 (History)
│   └── 点击 → 训练详情页
│
└── 设置 (Settings)
    ├── 数据导出/导入
    ├── AI 服务配置（API Key 等）
    └── 关于
```

---

## 6. 已决事项 & 待讨论

### ✅ 已定（全部核心决策完成）
- [x] **AI 服务**: 用户自供多模态 API（audio-in），一次调用完成语音识别 + 结构化提取 + 日志生成
- [x] **输入方式**: 语音为主（多模态 AI），文字为辅（同 AI 解析），手动填表兜底
- [x] **动作库**: 不需要预设动作库，AI 自行提取动作名 + 推断训练部位
- [x] **训练计划**: 现阶段仅记录，预设计划推迟至后期版本
- [x] **训练模板**: 支持将训练存为模板快速复用，语音可引用模板名
- [x] **UI 主题**: 深色系唯一，配色 **藏蓝 × 铜金**（`#0F172A` + `#D4A574`），极简线条风格
- [x] **社交功能**: 无
- [x] **身体指标**: 前期不做，聚焦核心训练记录功能
- [x] **API Key 管理**: 设置页自行填入，Android Keystore 加密存储
- [x] **有氧数据**: 完全语音口述
- [x] **Widget**: 桌面小组件 2×1，显示本周训练天数 + 一键启动录音
- [x] **数据导出**: JSON 格式
- [x] **存储策略**: 本地优先（Room），数据模型预留云端同步字段，后期扩展
- [x] **云端同步**: 后期功能，本地数据模型先预留 `syncId`、`updatedAt` 字段

### ❓ 全部已决，无待讨论项 ✅

---

## 7. 开发阶段建议

| 阶段 | 内容 | 优先级 |
|------|------|--------|
| 阶段 | 内容 | 优先级 |
|------|------|--------|
| **Phase 1 — MVP** | 手动录入（力量+有氧）+ 文字AI解析 + 日历视图 + 基础图表 | P0 |
| **Phase 2 — 语音+AI** | 多模态 API 接入 + 音频录制管理 + AI 结果确认页 + 训练模板 | P0 |
| **Phase 3 — 分析增强** | 趋势图完善 + 体量统计 + 周/月报生成 | P1 |
| **Phase 4 — 体验** | Widget（2×1 录音小组件）+ JSON 导出/导入 | P2 |
| **远期** | 训练计划预设、身体指标追踪、云端同步 | P3 |

---

## 8. 技术待调研清单

| 调研项 | 说明 | 影响 |
|--------|------|------|
| 多模态 API 选型 | 用户提供的 API 能力边界：是否支持 JSON Mode / Structured Output？音频大小限制？ | 解析方案 + Prompt 设计 |
| Android 音频录制 | MediaRecorder vs AudioRecord，音频格式选择（多模态 API 兼容性） | 录音实现 |
| 音频追加/合并 | 分段录音后合并为一个音频文件，还是多段分别发送+上下文拼接 | 多段录音流程 |
| Jetpack Compose 图表库 | Vico / MPAndroidChart / 自定义 Canvas | 趋势图实现 |
| Room + TypeConverter | 复杂嵌套结构（Workout→Exercises）的存储方案 | 数据层设计 |
| Android Keystore 加密 | API Key 本地加密存储方案 | 设置页安全实现 |

---

*此文档会持续迭代，请在讨论中随时补充/修改。*
